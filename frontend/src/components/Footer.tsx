export default function Footer() {
  return (
    <footer className="border-t border-line bg-white">
      <div className="section-shell flex flex-col gap-2 py-8 text-sm text-muted md:flex-row md:items-center md:justify-between">
        <p className="font-semibold text-nusNavy">Movie Hit Predictor</p>
        <p>NUS MSBA portfolio project focused on business analytics decision support.</p>
      </div>
    </footer>
  );
}
